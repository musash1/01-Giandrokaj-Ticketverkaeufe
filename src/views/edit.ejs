<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link
        href="https://fonts.googleapis.com/css2?family=Kanit:wght@200&family=Lato:wght@300&family=Roboto:wght@300&display=swap"
        rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/salesRegister.style.css">
    <link rel="stylesheet" href="/style.css">
    <title>Document</title>
</head>

<body>
    <main>
        <form method="post">
            <div class="registerSalesForm">
                <label for="name">Name</label>
                <input type="text" id="name-edit" name="name" required class="form-control" placeholder="Name"
                    value="<%- ticket.name %>">

                <label for="email">E-Mail</label>
                <input type="email" id="email-edit" name="email" required class="form-control" placeholder="test@mail.ch"
                    value="<%- ticket.email %>">

                <label for="telefon">Telefonnummer</label>
                <input type="tel" id="telefon-edit" name="telefon" class="form-control" pattern="^[0-9\s+\/\-)(]*$" placeholder="078 605 01 02"
                    value="<%- ticket.telefon %>">
                <label for="zahlungsstatus">Zahlungsstatus</label>
                <div class="radio-full-form" name="zahlungsstatus">
                    <div>
                        <input type="radio" id="bezahlt-edit" name="zahlungsstatus" value="Bezahlt" <% if(ticket.zahlungsstatus == "Bezahlt"){ %> checked <% } %>  >
                        <label for="bezahlt">Bezahlt</label>
                    </div>
                    <div>
                        <input type="radio" id="nicht_bezahlt-edit" name="zahlungsstatus" value="Nicht bezahlt" <% if(ticket.zahlungsstatus == "Nicht bezahlt"){ %> checked <% } %>>
                        <label for="nicht_bezahlt">Nicht bezahlt</label>
                    </div>
                </div>
                <label for="treuebonus">Treuebonus</label>
                <select name="treuebonus" id="treuebonus-edit" class="form-select">
                    <option value="30" <% if (ticket.treuebonus == 30){ %> selected <% } %>>Kein Rabatt</option>
                    <option value="20" <% if (ticket.treuebonus == 20){ %> selected <% } %>>5% Rabatt</option>
                    <option value="15" <% if (ticket.treuebonus == 15){ %> selected <% } %>>10% Rabatt</option>
                    <option value="10" <% if (ticket.treuebonus == 10){ %> selected <% } %>>15% Rabatt</option>
                </select>

                <label for="konzert">Konzert</label>
                <select id="konzert-edit" class="form-select" name="konzert">
                    <% for (let i=0; i < concerts.length; i++){ %>
                        <option value="<%- concerts[i].id %> ">
                            <%- concerts[i].artist %>
                        </option>
                        <% } %>
                </select>
            </div>
            <div class="ticket-buttons">
                <a href="/ticket-anzeigen" class="button-link"><button onclick=""
                        class='btn btn-danger button' type="button">Abbrechen</button></a>
                <button class="btn btn-primary button" type="submit">Verkauf speichern</button>
            </div>

        </form>
    </main>
</body>

</html>